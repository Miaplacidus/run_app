$(document).ready(function(){

  $('.postcreate_modal .modal-body').html("<%= escape_javascript(render 'postconfirm') %>");
  $('.postcreate_modal .submit').hide();

  $('.postcreate_modal .cls_btn').on('click', function() {
    $('.postcreate_modal .modal-body').html("<%= escape_javascript(render 'postcreate') %>");
    $('.poscreate_modal .submit').show();
  });

  $('#postcreatemodal').on('hidden.bs.modal', function () {
    $('.postcreate_modal .modal-body').html("<%= escape_javascript(render 'postcreate') %>");
    $('#postcreatemodal .submit').show();
  })

});
